package dp.angryballs.modele.states;

import dp.angryballs.modele.Bille;

/**
 * État par défaut d'une bille
 */
public class DefaultState extends StateBille {
    public DefaultState(Bille parent) {
        super(parent);
    }
}
